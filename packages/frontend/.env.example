# Frontend environment variables (Vite)
# Copy to .env.local for local development.

# Backend API URL — used by the axios client base URL.
# In Docker Compose dev: http://localhost:3001/api
# When unset, the client falls back to "/api" (works behind a reverse proxy).
VITE_API_URL=http://localhost:3001/api
